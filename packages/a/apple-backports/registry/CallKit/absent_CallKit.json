{
  "framework": "CallKit",
  "entries": [
    {
      "api": "+[CXProvider reportNewIncomingVoIPPushPayload:completion:]",
      "kind": "method",
      "introduced": "14.5",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the method exists to turn a PKPushRegistry VoIP push payload into a reported call, and this port never receives one to turn: apsd on iOS 6.1.3, measured against real hardware (facts/PushKit/PushKit.md), ties every device token to a nonzero UIRemoteNotificationType bitmask (Badge/Sound/Alert) with no silent fourth bit, so a PKPushRegistry set up the way PushKit is meant to be used - with no prior -registerUserNotificationSettings: call - never receives a token at all; and apsd never wakes a suspended or not-running application for any push regardless. The wall is Apple's own push daemon on this release, not a version gate",
      "effect": "the method is not there: respondsToSelector: answers NO and a compile time call crashes",
      "source": "SDK 16.4, CallKit"
    },
    {
      "api": "CXCallDirectoryExtensionContext",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the class is the execution context an app extension target runs in, and iOS 6 has no app-extension infrastructure to load one into (facts/Foundation/CloudAndExtensions.md: no host, NSExtensionMain answers 69). The wall is the missing loader, not the class's own age",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "SDK 16.4, CallKit"
    },
    {
      "api": "CXCallDirectoryExtensionContextDelegate",
      "kind": "protocol",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the protocol is the callback surface CXCallDirectoryExtensionContext calls into, and that context is never created: iOS 6 has no app-extension infrastructure to load one into (facts/Foundation/CloudAndExtensions.md). The wall is the missing loader, not the protocol's own age",
      "effect": "the protocol is not there: NSProtocolFromString answers nil",
      "source": "SDK 16.4, CallKit"
    },
    {
      "api": "CXCallDirectoryManager",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the class is how a host application enables, disables and reloads its own call directory extensions, and iOS 6 has no app-extension infrastructure to host one at all (facts/Foundation/CloudAndExtensions.md): there is nothing for this class to manage",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "SDK 16.4, CallKit"
    },
    {
      "api": "CXCallDirectoryProvider",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the class is the principal class an app-extension target subclasses, and iOS 6 has no app-extension infrastructure to instantiate one (facts/Foundation/CloudAndExtensions.md: no host, NSExtensionMain answers 69). The wall is the missing loader, not the class's own age",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "SDK 16.4, CallKit"
    }
  ]
}
