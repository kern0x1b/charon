{
  "framework": "Foundation",
  "entries": [
    {
      "api": "NSURLSessionTaskMetrics",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's own session engine over NSURLConnection is the only thing that can say when a load began and ended",
      "effect": "a task's metrics: its transactions, one for the request and one for each redirect, the interval from the creation of the task to its completion, and the count of redirects",
      "facts": "facts/Foundation/NSURLSessionTaskMetrics.md",
      "source": "the host's own Foundation, held against the port by the metrics scenario of tests/backports/host/session (the system and the port compared in one process) and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "NSURLSessionTaskTransactionMetrics",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's own session engine over NSURLConnection is the only thing that can say when a load began and ended",
      "effect": "the request, the response, the fetch, request and response dates the engine saw, the fetch type and the protocol name; the dates of the name lookup and of the connection, the two flags for a proxy and a reused connection, and everything that arrived after iOS 10 are not carried",
      "facts": "facts/Foundation/NSURLSessionTaskMetrics.md",
      "source": "the host's own Foundation, held against the port by the metrics scenario of tests/backports/host/session (the system and the port compared in one process) and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "-[NSURLSessionTaskDelegate URLSession:task:didFinishCollectingMetrics:]",
      "kind": "method",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's own session engine over NSURLConnection is the only thing that can say when a load began and ended",
      "effect": "sent to the session's delegate on its queue once for every task, after the last data and before URLSession:task:didCompleteWithError: and the completion handler, for a task that finished, failed or was cancelled",
      "facts": "facts/Foundation/NSURLSessionTaskMetrics.md",
      "source": "the host's own Foundation, held against the port by the metrics scenario of tests/backports/host/session (the system and the port compared in one process) and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "NSURLSessionDelegate",
      "kind": "protocol",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's session sends the messages of the protocol itself",
      "effect": "the port's session sends the messages the scenarios of tests/backports/device/session-scenarios.m exercise, on the delegate queue, in the order the system does",
      "facts": "facts/Foundation/NSURLSession.md",
      "source": "the host's own Foundation, held against the port by tests/backports/host/session and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "NSURLSessionTaskDelegate",
      "kind": "protocol",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's session sends the messages of the protocol itself",
      "effect": "the port's session sends the messages the scenarios of tests/backports/device/session-scenarios.m exercise, on the delegate queue, in the order the system does",
      "facts": "facts/Foundation/NSURLSession.md",
      "source": "the host's own Foundation, held against the port by tests/backports/host/session and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "NSURLSessionDataDelegate",
      "kind": "protocol",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's session sends the messages of the protocol itself",
      "effect": "the port's session sends the messages the scenarios of tests/backports/device/session-scenarios.m exercise, on the delegate queue, in the order the system does",
      "facts": "facts/Foundation/NSURLSession.md",
      "source": "the host's own Foundation, held against the port by tests/backports/host/session and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    },
    {
      "api": "NSURLSessionDownloadDelegate",
      "kind": "protocol",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has no NSURLSession, so the port's session sends the messages of the protocol itself",
      "effect": "the port's session sends the messages the scenarios of tests/backports/device/session-scenarios.m exercise, on the delegate queue, in the order the system does",
      "facts": "facts/Foundation/NSURLSession.md",
      "source": "the host's own Foundation, held against the port by tests/backports/host/session and read again on an iPhone 4S and an iPad 2 running 6.1.3 by tests/backports/device/session.m"
    }
  ]
}
