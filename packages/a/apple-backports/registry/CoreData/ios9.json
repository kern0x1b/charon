{
  "framework": "CoreData",
  "entries": [
    {
      "api": "NSPersistentStoreResult",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "the base class of what a request answers, with nothing in it",
      "facts": "facts/CoreData/BatchDelete.md",
      "source": "CoreData of the arm64 shared cache of iOS 12.0: the class is an empty subclass of NSObject"
    },
    {
      "api": "NSBatchDeleteRequest",
      "kind": "class",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "made with a fetch request, which it keeps as a copy that asks for object IDs alone, without property values, pending changes or a batch size; a nil fetch request or one with no entity is refused with an NSInvalidArgumentException; request type 7; the result type starts as status only and keeps any value it is given",
      "facts": "facts/CoreData/BatchDelete.md",
      "source": "CoreData of the arm64 shared cache of iOS 12.0: -[NSBatchDeleteRequest initWithFetchRequest:] at 0x183a878e0, -requestType at 0x183c28800 and the description format; the host's Core Data for the copy"
    },
    {
      "api": "-[NSBatchDeleteRequest initWithObjectIDs:]",
      "kind": "method",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "a request that fetches the given objects with SELF IN; an empty array, and object IDs of more than one root entity, are refused with an NSInvalidArgumentException that carries the object IDs",
      "facts": "facts/CoreData/BatchDelete.md",
      "source": "CoreData of the arm64 shared cache of iOS 12.0: -[NSBatchDeleteRequest initWithObjectIDs:] at 0x183c28570, which reads the root entity of the last object ID, checks the rest against it, builds the fetch request and hands it to -initWithFetchRequest:"
    },
    {
      "api": "NSBatchDeleteResult",
      "kind": "class",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "holds the result and the result type it was made with, which are nil and status only when it is made with init: status only answers YES, object IDs the array of the ones removed, count the number",
      "facts": "facts/CoreData/BatchDelete.md",
      "source": "CoreData of the arm64 shared cache of iOS 12.0: -[NSBatchDeleteResult initWithResultType:andObject:] and its accessors"
    },
    {
      "api": "-[NSManagedObjectContext executeRequest:error:]",
      "kind": "method",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "a departure from 12.0 in how a batch delete is carried out and in what other requests answer: iOS 6 has no SQL for it, so the fetch request runs in a private context of its own, the objects it finds are deleted and saved, which applies their delete rules and validation and posts a did save notification of that context, none of which iOS 12 does; a fetch request given to it is executed as -executeFetchRequest:error: and answers the array, where iOS 12 wraps it in an asynchronous fetch result, and any other request is executed by the release's own coordinator and answers what that answers",
      "effect": "a batch delete request removes what its fetch request finds from the store, and the objects of the calling context are left as they are, neither marked deleted nor refreshed; it answers a batch delete result of the request's result type, nil and the error when the fetch or the save fails; a fetch request answers the array of its results; any other request goes to the release's own coordinator",
      "facts": "facts/CoreData/BatchDelete.md",
      "source": "CoreData of the arm64 shared cache of iOS 12.0: -[NSManagedObjectContext executeRequest:error:] at 0x1839f0f58 reads the request type and hands a batch delete to the coordinator; the host's Core Data, run beside the port on a twin store, for what a batch delete removes and answers"
    }
  ]
}
