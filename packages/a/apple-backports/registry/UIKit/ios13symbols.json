{
  "framework": "UIKit",
  "entries": [
    {
      "api": "UIImageConfiguration",
      "kind": "class",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "an immutable holder of a trait collection that is copied, compared and archived as UIKit's is; iOS 6 draws no symbol, so nothing but the application reads it",
      "facts": "facts/UIKit/UIImageSymbolConfiguration.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "UIImageSymbolConfiguration",
      "kind": "class",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "an immutable holder of a point size or text style, weight, scale and traits that is built, combined, compared, described and archived as UIKit's is; the values are kept and read back, and no symbol image exists on iOS 6 to be drawn by them",
      "facts": "facts/UIKit/UIImageSymbolConfiguration.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "-[UIImageConfiguration init]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the header marks it unavailable; a call made at run time reaches NSObject and gives a configuration with nothing specified, as the host does",
      "effect": "a call made at run time reaches NSObject's -init and gives a configuration with nothing specified, as the host's does; nothing can be written against it in a modern SDK",
      "facts": "facts/UIKit/UIImageSymbolConfiguration.md",
      "source": "the header of SDK 16.4 and the host's own UIKit"
    },
    {
      "api": "+[UIImageConfiguration new]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the header marks it unavailable; a call made at run time reaches NSObject and gives a configuration with nothing specified, as the host does",
      "effect": "a call made at run time reaches NSObject's +new and gives a configuration with nothing specified, as the host's does; nothing can be written against it in a modern SDK",
      "facts": "facts/UIKit/UIImageSymbolConfiguration.md",
      "source": "the header of SDK 16.4 and the host's own UIKit"
    },
    {
      "api": "UIImageSymbolWeightForFontWeight()",
      "kind": "function",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "answers the symbol weight whose float boundary the font weight reaches, Unspecified below the lightest and Black from 0.62 up",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "UIFontWeightForImageSymbolWeight()",
      "kind": "function",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the host reads memory beside its table for a symbol weight below zero, which is not carried over",
      "effect": "answers the font weight of the table for 0 to 9 and 0 for any other symbol weight",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "UIImage.symbolConfiguration",
      "kind": "property",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "the configuration of a drawn symbol, the one given merged over the traits of the screen; nil for an ordinary image nobody applied one to, and the applied one for an ordinary image made by -imageByApplyingSymbolConfiguration:",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "-[UIImage isSymbolImage]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "iOS 6 has no SF Symbols, so no image is a symbol image",
      "effect": "-isSymbolImage answers NO for every image",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "-[UIImage imageByApplyingSymbolConfiguration:]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "draws a symbol image again with the merged configuration; for an ordinary image answers a copy that holds the merged configuration and changes nothing that is drawn",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "+[UIImage systemImageNamed:]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "iOS 6 has no SF Symbols, so the port draws its own glyphs for a listed set of names; a name outside the set is nil, as a name the system does not know is",
      "effect": "answers a template image drawn by the port for each of 549 names (the 251 found in the corpus and 298 common ones), at the size, alignment insets and baseline the host gives, and nil for every other name, among them the names of the absent-symbols list of the facts file, which the host knows and the port does not draw yet",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "+[UIImage systemImageNamed:compatibleWithTraitCollection:]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "iOS 6 has no SF Symbols, so the port draws its own glyphs for a listed set of names; a name outside the set is nil, as a name the system does not know is",
      "effect": "answers a template image drawn by the port for each of 549 names (the 251 found in the corpus and 298 common ones), at the size, alignment insets and baseline the host gives, and nil for every other name, among them the names of the absent-symbols list of the facts file, which the host knows and the port does not draw yet",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "+[UIImage systemImageNamed:withConfiguration:]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "iOS 6 has no SF Symbols, so the port draws its own glyphs for a listed set of names; a name outside the set is nil, as a name the system does not know is",
      "effect": "answers a template image drawn by the port for each of 549 names (the 251 found in the corpus and 298 common ones), at the size, alignment insets and baseline the host gives, and nil for every other name, among them the names of the absent-symbols list of the facts file, which the host knows and the port does not draw yet",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    },
    {
      "api": "UIImageView.preferredSymbolConfiguration",
      "kind": "property",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "a view draws a symbol image again when the configuration or the image changes, since iOS 6 has no symbol rendering of its own",
      "effect": "kept as given, an equal one leaving the first in place; a symbol image put in the view, or already in it, is drawn again with the image's own configuration over this one (the view then holds that drawing as its image); an ordinary image is left as it is",
      "facts": "facts/UIKit/UIImageSymbols.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/uikit2 (the symbols group); the header of SDK 16.4"
    }
  ]
}
