{
  "framework": "UIKit",
  "entries": [
    {
      "api": "UICollectionViewLayoutInvalidationContext",
      "kind": "class",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "holds the index paths, the adjustments and the flags as UIKit's does; the release never makes one, an application does",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    },
    {
      "api": "UICollectionViewFlowLayoutInvalidationContext",
      "kind": "class",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "adds the two flow flags, both yes at first; the release's flow layout does not read them",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    },
    {
      "api": "+[UICollectionViewLayout invalidationContextClass]",
      "kind": "method",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "answers the base context class",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    },
    {
      "api": "+[UICollectionViewFlowLayout invalidationContextClass]",
      "kind": "method",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "answers the flow context class",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    },
    {
      "api": "-[UICollectionViewLayout invalidationContextForBoundsChange:]",
      "kind": "method",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "answers a new context of the layout's class, marking nothing, as UIKit's does for a layout that does not override it",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    },
    {
      "api": "-[UICollectionViewLayout invalidateLayoutWithContext:]",
      "kind": "method",
      "introduced": "7.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release's collection view calls -invalidateLayout and nothing else, so it never builds a context or hands one to a layout",
      "effect": "invalidates the layout as -invalidateLayout does; the context's contents are not read, the release having no partial invalidation. -invalidateLayout, called by the release or by an application, now calls this method with a new context of the layout's class first, so an override of it is called",
      "facts": "facts/UIKit/UICollectionViewInvalidation.md",
      "source": "the host's own UIKit under Mac Catalyst (macOS 27.0), held against the backport by tests/backports/host/invalidation; the header of SDK 16.4"
    }
  ]
}
