{
  "framework": "Metal",
  "entries": [
    {
      "api": "MTLTextureDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLSamplerDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassAttachmentDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassColorAttachmentDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassColorAttachmentDescriptorArray",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPipelineColorAttachmentDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPipelineColorAttachmentDescriptorArray",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPipelineDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes a texture, a sampler, a render pass or a pipeline the way it does with Metal; the port draws them with OpenGL ES 2.0, the only graphics the A5 has",
      "effect": "an object that keeps the properties it is given; a value the port cannot draw with is refused where it is used, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLDevice",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLResource",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLBuffer",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLTexture",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLSamplerState",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLFunction",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLLibrary",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPipelineState",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLCommandQueue",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLCommandBuffer",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLCommandEncoder",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderCommandEncoder",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLDrawable",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried by the classes of the port that answer for it; an application that holds an id of it gets an object of OpenGL ES 2.0 behind it",
      "effect": "the protocol is there, and the methods of it the port does not do are not implemented; facts/Metal/RenderPath.md lists what is done and what is not",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLVertexAttributeDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes how its vertex buffers feed a vertex function that reads its inputs by attribute, the way it does with Metal; the port binds them as OpenGL ES 2.0 vertex attributes",
      "effect": "an object that keeps the properties it is given; a format or step function the port cannot draw with is refused when the pipeline is made, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLVertexAttributeDescriptorArray",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes how its vertex buffers feed a vertex function that reads its inputs by attribute, the way it does with Metal; the port binds them as OpenGL ES 2.0 vertex attributes",
      "effect": "an object that keeps the properties it is given; a format or step function the port cannot draw with is refused when the pipeline is made, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLVertexBufferLayoutDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes how its vertex buffers feed a vertex function that reads its inputs by attribute, the way it does with Metal; the port binds them as OpenGL ES 2.0 vertex attributes",
      "effect": "an object that keeps the properties it is given; a format or step function the port cannot draw with is refused when the pipeline is made, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLVertexBufferLayoutDescriptorArray",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes how its vertex buffers feed a vertex function that reads its inputs by attribute, the way it does with Metal; the port binds them as OpenGL ES 2.0 vertex attributes",
      "effect": "an object that keeps the properties it is given; a format or step function the port cannot draw with is refused when the pipeline is made, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLVertexDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application describes how its vertex buffers feed a vertex function that reads its inputs by attribute, the way it does with Metal; the port binds them as OpenGL ES 2.0 vertex attributes",
      "effect": "an object that keeps the properties it is given; a format or step function the port cannot draw with is refused when the pipeline is made, as facts/Metal/RenderPath.md lists",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLFunctionConstantValues",
      "kind": "class",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application specialises a function of its library the way it does with Metal; the port turns the values into definitions the shader is compiled with",
      "effect": "an object that keeps the bool, int, unsigned and float values it is given by index and by name; a value of another type than the constant's is an error when the function is made",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLDepthStencilDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application tests and writes depth and stencil the way it does with Metal; the port does it with the depth and stencil buffers OpenGL ES 2.0 has",
      "effect": "an object that keeps the properties it is given; the depth of Metal's clip space is turned into ES's by the vertex shader, and what ES 2.0 cannot do is listed in facts/Metal/RenderPath.md",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLDepthStencilState",
      "kind": "protocol",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the protocol is carried so that an application tests and writes depth and stencil the way it does with Metal; the port does it with the depth and stencil buffers OpenGL ES 2.0 has",
      "effect": "the protocol is there, and the state it names is made by the device and set on the render command encoder; the depth of Metal's clip space is turned into ES's by the vertex shader, and what ES 2.0 cannot do is listed in facts/Metal/RenderPath.md",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassDepthAttachmentDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application tests and writes depth and stencil the way it does with Metal; the port does it with the depth and stencil buffers OpenGL ES 2.0 has",
      "effect": "an object that keeps the properties it is given; the depth of Metal's clip space is turned into ES's by the vertex shader, and what ES 2.0 cannot do is listed in facts/Metal/RenderPath.md",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLRenderPassStencilAttachmentDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application tests and writes depth and stencil the way it does with Metal; the port does it with the depth and stencil buffers OpenGL ES 2.0 has",
      "effect": "an object that keeps the properties it is given; the depth of Metal's clip space is turned into ES's by the vertex shader, and what ES 2.0 cannot do is listed in facts/Metal/RenderPath.md",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    },
    {
      "api": "MTLStencilDescriptor",
      "kind": "class",
      "introduced": "8.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the class is carried so that an application tests and writes depth and stencil the way it does with Metal; the port does it with the depth and stencil buffers OpenGL ES 2.0 has",
      "effect": "an object that keeps the properties it is given; the depth of Metal's clip space is turned into ES's by the vertex shader, and what ES 2.0 cannot do is listed in facts/Metal/RenderPath.md",
      "facts": "facts/Metal/RenderPath.md",
      "source": "SDK 16.4, Metal; the behaviour is the one its header documents, and was not read from a shared cache"
    }
  ]
}
