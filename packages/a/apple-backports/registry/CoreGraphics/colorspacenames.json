{
  "framework": "CoreGraphics",
  "entries": [
    {
      "api": "kCGColorSpaceACESCGLinear",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceDCIP3",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceDisplayP3",
      "kind": "constant",
      "introduced": "9.3",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceDisplayP3_PQ",
      "kind": "constant",
      "introduced": "13.4",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedDisplayP3",
      "kind": "constant",
      "introduced": "14.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedGray",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedITUR_2020",
      "kind": "constant",
      "introduced": "14.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedLinearGray",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedLinearSRGB",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceExtendedSRGB",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceGenericXYZ",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_2020",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_2020_PQ",
      "kind": "constant",
      "introduced": "13.4",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, the string kCGColorSpaceITUR_2100_PQ; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_2020_sRGBGamma",
      "kind": "constant",
      "introduced": "15.1",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_2100_HLG",
      "kind": "constant",
      "introduced": "14.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_2100_PQ",
      "kind": "constant",
      "introduced": "14.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_709",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_709_HLG",
      "kind": "constant",
      "introduced": "15.1",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceITUR_709_PQ",
      "kind": "constant",
      "introduced": "15.1",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceLinearDisplayP3",
      "kind": "constant",
      "introduced": "15.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceLinearGray",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceLinearITUR_2020",
      "kind": "constant",
      "introduced": "15.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceLinearSRGB",
      "kind": "constant",
      "introduced": "10.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    },
    {
      "api": "kCGColorSpaceROMMRGB",
      "kind": "constant",
      "introduced": "9.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the symbol is carried so that an application which names it loads; iOS 6 cannot make a space of this name",
      "effect": "the constant is there with the value the host's CoreGraphics gives it, a string equal to its own name; CGColorSpaceCreateWithName answers NULL for it, as it does for every name of the release that it cannot make, and an application that checks the result handles it",
      "facts": "facts/CoreGraphics/CGColorSpace.md",
      "source": "CoreGraphics of the macOS host, which holds the string of the constant; an iPad 2 running 6.1.3, where CGColorSpaceCreateWithName answers NULL for the names the release does not make and has no such symbol"
    }
  ]
}
