{
  "framework": "AuthenticationServices",
  "entries": [
    {
      "api": "ASWebAuthenticationSession",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "a departure from 12.0 in the start: iOS 12 starts the wrapped session through a method of its own that this release does not have, so the session is started as the SFAuthenticationSession of iOS 11 is, over the page of this package",
      "effect": "the session is made with a URL, a callback scheme and a completion handler, and is started with -start, which answers YES once and NO while it runs; the handler is given the callback URL with no error when the page reaches the scheme, or nil and an error of ASWebAuthenticationSessionErrorDomain with the code 1 for a cancel, for an address that is not a web address and for a page dismissed by the user; -cancel ends a running session the same way and does nothing to one that has not started",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "AuthenticationServices of the arm64 shared cache of iOS 12.0: -[ASWebAuthenticationSession initWithURL:callbackURLScheme:completionHandler:] at 0x1aa076000 makes an SFAuthenticationSession whose handler hands the callback URL on and turns any error into code 1 of the domain, -start and -cancel at 0x1aa076200 and 0x1aa076218 forward to it; an iPad 2 running 6.1.3 with a loopback server for what comes out"
    },
    {
      "api": "ASWebAuthenticationSessionErrorDomain",
      "kind": "constant",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "the string com.apple.AuthenticationServices.WebAuthenticationSession, which iOS 12 gives the constant",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "AuthenticationServices of the arm64 shared cache of iOS 12.0: the string the error of the session is made with, at 0x1aa07c65e"
    },
    {
      "api": "ASCredentialIdentityStore",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASCredentialIdentityStoreState",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASCredentialProviderExtensionContext",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASCredentialProviderViewController",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASCredentialServiceIdentifier",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASPasswordCredential",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASPasswordCredentialIdentity",
      "kind": "class",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "the release has no credential provider extension or identity store for the system's AutoFill to consult, and a password that only looks kept would mislead",
      "effect": "the class is not there: NSClassFromString answers nil and alloc on a compile time reference crashes",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASCredentialIdentityStoreErrorDomain",
      "kind": "constant",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "it names the errors of the credential provider, which is absent",
      "effect": "the symbol is not there: a weak reference to it is NULL",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASExtensionErrorDomain",
      "kind": "constant",
      "introduced": "12.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "it names the errors of the credential provider, which is absent",
      "effect": "the symbol is not there: a weak reference to it is NULL",
      "source": "the SDK 16.4 headers of AuthenticationServices"
    },
    {
      "api": "ASWebAuthenticationSession.presentationContextProvider",
      "kind": "property",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the provider exists so the release can pick the window to present the page over; -start now asks it, the way the release does",
      "effect": "kept and read back weakly, as the release holds it; -start sends presentationAnchorForWebAuthenticationSession: and presents over the anchor it returns. A session with no provider, or a provider that returns nil, still starts over the one window this release runs, where iOS 13 raises for a missing provider",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "the headers of SDK 16.4; AuthenticationServices of the arm64 shared cache of iOS 13.0 for what the release does, and the SFAuthenticationSession of iOS 11 this package's session is built on for what it can do"
    },
    {
      "api": "ASWebAuthenticationPresentationContextProviding",
      "kind": "protocol",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "an application's own class adopts this protocol; the port's part is sending its one message, not declaring the protocol itself, which the SDK's own headers already do",
      "effect": "-[ASWebAuthenticationSession start] sends presentationAnchorForWebAuthenticationSession: to whatever conforms to it",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "the headers of SDK 16.4; AuthenticationServices of the arm64 shared cache of iOS 13.0 for what the release does, and the SFAuthenticationSession of iOS 11 this package's session is built on for what it can do"
    },
    {
      "api": "-[ASWebAuthenticationPresentationContextProviding presentationAnchorForWebAuthenticationSession:]",
      "kind": "method",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the port carries the caller's side of this protocol member: -[ASWebAuthenticationSession start] sends it to the provider, the same way the release does, and presents over the window it returns instead of always guessing the key window",
      "effect": "an application's own provider is asked, and its answer is used to choose which window the sign-in page appears over",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "the headers of SDK 16.4; AuthenticationServices of the arm64 shared cache of iOS 13.0 for what the release does, and the SFAuthenticationSession of iOS 11 this package's session is built on for what it can do"
    },
    {
      "api": "ASWebAuthenticationSession.prefersEphemeralWebBrowserSession",
      "kind": "property",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "implemented",
      "reason": "the release has one process-wide cookie jar and no store per UIWebView, so isolating a sign-in flow means emptying that jar for the flow's duration, not switching to a private store. The jar is journaled to a plist on disk before it is emptied (a jetsam kill mid-flow must not cost the application every cookie it owned), replaced empty, and restored from the journal when the flow ends by any path - completion, cancellation, or a fresh launch finding a leftover journal from a kill",
      "effect": "starting an ephemeral session empties the shared cookie jar for the flow and restores exactly what was there before once it ends; cookies and the URL cache are isolated, local and database storage are not, since UIWebView has no such per-session store to isolate on this release",
      "source": "the headers of SDK 16.4; AuthenticationServices of the arm64 shared cache of iOS 13.0 for what the release does, and the SFAuthenticationSession of iOS 11 this package's session is built on for what it can do",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md"
    },
    {
      "api": "ASWebAuthenticationSession.canStart",
      "kind": "property",
      "introduced": "13.4",
      "minimum": "6.0",
      "status": "implemented",
      "effect": "YES until the session has been started or cancelled, NO after, which is what a session that has been spent answers; it is read from the port's own state, since SFAuthenticationSession has nothing to ask",
      "facts": "facts/AuthenticationServices/ASWebAuthenticationSession.md",
      "source": "the headers of SDK 16.4; AuthenticationServices of the arm64 shared cache of iOS 13.0 for what the release does, and the SFAuthenticationSession of iOS 11 this package's session is built on for what it can do"
    },
    {
      "api": "ASAuthorizationAppleIDCredential.authorizationCode",
      "kind": "property",
      "introduced": "13.0",
      "minimum": "6.0",
      "status": "absent",
      "reason": "Sign in with Apple is not carried: the release has no Apple ID authorization service, no ASAuthorization controller and no way to reach the identity provider, so there is no credential to read a code from",
      "effect": "the class and the property are not there",
      "source": "the headers of SDK 16.4; nothing of a release was read, as the release has none of the classes"
    }
  ]
}
